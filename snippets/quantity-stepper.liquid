{%- comment -%}
  Quantity Stepper Snippet
  Used by: snippets/bundle-product-card.liquid — quantity increment/decrement controls

  Expected variables:
    - product  {Object}  Shopify product object (required)
{%- endcomment -%}

<div class="sd-frame-8234__quantity-stepper" data-product-id="{{ product.id }}">
  <button
    type="button"
    class="sd-frame-8234__qty-btn sd-frame-8234__qty-btn--minus"
    aria-label="{{ 'sections.sd_frame_8234.decrease_qty' | t | default: 'Decrease quantity of' }} {{ product.title }}"
    data-action="decrement"
    data-product-id="{{ product.id }}"
  >
    <svg width="12" height="2" viewBox="0 0 12 2" fill="none" aria-hidden="true">
      <path d="M1 1H11" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
    </svg>
  </button>

  <span
    class="sd-frame-8234__qty-value"
    data-qty-display="{{ product.id }}"
    aria-live="polite"
    aria-label="{{ 'sections.sd_frame_8234.quantity' | t | default: 'Quantity' }}"
  >0</span>

  <button
    type="button"
    class="sd-frame-8234__qty-btn sd-frame-8234__qty-btn--plus"
    aria-label="{{ 'sections.sd_frame_8234.increase_qty' | t | default: 'Increase quantity of' }} {{ product.title }}"
    data-action="increment"
    data-product-id="{{ product.id }}"
  >
    <svg width="12" height="12" viewBox="0 0 12 12" fill="none" aria-hidden="true">
      <path d="M6 1V11M1 6H11" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
    </svg>
  </button>
</div>